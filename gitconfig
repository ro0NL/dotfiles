[user]
	name = Roland Franssen
	email = franssen.roland@gmail.com
	signingkey = 366BB49520DA6261
[core]
	excludesfile = ~/.gitignore
	ignorecase = false
	autocrlf = input
[color]
	status = auto
	diff = auto
	branch = auto
[push]
	default = simple
[pull]
	rebase = true
[alias]
	co = checkout
	cm = commit
	cma = commit --amend
	cp = cherry-pick
	br = branch
	me = merge
	rb = rebase
	st = status
	sm = submodule
	sminit = submodule update --init
	lol = log --graph --decorate --pretty=format:'%C(yellow)%h%Creset%C(auto)%d%Creset %s %Cgreen(%ci) %C(bold blue)<%an>%Creset' --abbrev-commit --all
	adda = add --all
	unstage = reset HEAD
	dif = diff
	difc = diff --cached

